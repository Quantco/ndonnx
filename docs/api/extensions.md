# Extensions beyond the array-api

The `ndonnx.extensions` module contains various functions which are not part of the array-api.
The functions are maintained in this module to reduce the risk of breaking changes when functions of the same name are added to the array-api in the future.

<!-- prettier-ignore -->
::: ndonnx.extensions
    options:
      show_root_members_full_path: true
      filters:
        - "!^_"
